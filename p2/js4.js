const numbs = [1,2,3,4,5,6,7,8,9,0]

// const newnumbs= numbs.map( (number) => {return number + 10 })

// channing
// const newnumbs = numbs.map( (number) => number*10 )
// .map( (number) => number + 1 )  // === here the value passed is not 1 it will be number*10 vala value
// .filter( (number) =>number>=41)  // can use multiple methods
// console.log(newnumbs);

const newarr = [1,2,3]

const arr1=newarr.reduce( (acc,current) => {
    // console.log(`acc${acc} and curr${current}`);
    // return acc + current
},0)

// console.log(arr1);






